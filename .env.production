# This file is committed to git (see .gitignore — only `.env.production.local`
# is excluded, not this bare filename). Never put secrets here, only
# non-sensitive values like this URL.
BACKEND_URL=https://travelbackend-production-acf6.up.railway.app
